# ============================================================
# CUE — environment
# ============================================================
# Local mode: leave everything blank. The app uses localStorage.
# Supabase mode: fill both and follow supabase-schema.sql once.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
